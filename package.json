{
  "name": "winrect",
  "private": true,
  "version": "0.1.0",
  "description": "Rectangle-style window tiling for Windows (Tauri)",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
